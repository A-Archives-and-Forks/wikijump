name: '[backend] Docker build router (prod)'

on:
  pull_request:
    paths:
      - 'deepwell/**'
      - 'install/prod/router/Dockerfile'
      - '.github/workflows/docker-build-router.prod.yaml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Build image
        run: docker build -f install/prod/router/Dockerfile .
        env:
          DOCKER_BUILDKIT: 1
