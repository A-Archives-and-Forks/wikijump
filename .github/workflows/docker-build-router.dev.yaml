name: '[backend] Docker build router (dev)'

on:
  pull_request:
    paths:
      - 'deepwell/**'
      - 'install/dev/router/Dockerfile'
      - '.github/workflows/docker-build-router.dev.yaml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Build image
        run: docker build -f install/dev/router/Dockerfile .
        env:
          DOCKER_BUILDKIT: 1
