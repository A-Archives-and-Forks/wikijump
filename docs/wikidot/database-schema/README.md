## Wikidot Database Schema

This directory contains the database schemas used by Wikidot in its PostgreSQL database. They are present here as a reference only.

For the database schemas used in Wikijump, see `deepwell/migrations/`.
